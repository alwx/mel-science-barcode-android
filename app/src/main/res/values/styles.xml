<resources>
    <style name="Theme.MELScience" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>

        <item name="melHover">@drawable/hover</item>
    </style>

    <style name="Theme.MELScience.Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
    </style>

    <!-- Widgets -->

    <style name="Theme.MELScience.Text.Default" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/text_size_default</item>
    </style>

    <style name="Theme.MELScience.Text.Big" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/text_size_big</item>
    </style>

    <style name="FloatingActionButton">
        <item name="android:layout_margin">0dp</item>
    </style>

</resources>
